import { ShellContext } from '@/components/shell'
import { Box } from '@mantine/core'
import { useContext } from 'react'

export default function Dashboard() {
  const { setMenu } = useContext(ShellContext)

  return <Box></Box>
}